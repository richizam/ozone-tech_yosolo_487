# environments & caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# simulation outputs (regenerable; keep them out of the submission repo)
runs/
MUJOCO_LOG.TXT
# sign textures, rendered on demand by cell/signs.py
cell/assets/signs/

# ML training artifacts (regenerable via perception/synth_dataset.py + yolo_compare.py;
# final weights go to cloud storage per the submission rules)
datasets/
*.pt
*.onnx

# large organizer binaries: the extracted STL working set stays in git;
# STEP originals and the source zips go to cloud storage per the submission rules
extracted/**/Step/
doc-*.zip
