# environments & caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# simulation outputs (regenerable; keep them out of the submission repo)
runs/
MUJOCO_LOG.TXT
# sign textures, rendered on demand by cell/signs.py
cell/assets/signs/

# ML training artifacts (regenerable via perception/synth_dataset.py + yolo_compare.py;
# final weights go to cloud storage per the submission rules)
datasets/
*.pt
*.onnx

# large organizer binaries: the extracted STL working set stays in git;
# STEP originals and the source zips go to cloud storage per the submission rules
extracted/**/Step/
doc-*.zip

# expert-provided items (generated locally by tools/expert_check.py)
cell/assets/manifest_custom.json
cell/assets/meshes/custom_*.stl

# fetch scratch area (canonical committed set: videos_final/)
docs/report/isaac_evidence/xbelt/videos/

# guion de trabajo del video (documento interno, no entregable)
docs/report/video_script.md

# debug scratch de contenedor (reproducciones puntuales)
out_dbg/

# ---- deck build scratch -------------------------------------------------
# entregable = YOSOLO_stage1_pitch_ru_FINAL.pptx (en la raíz, SÍ se sube).
# Todo lo demás alrededor del deck es material de trabajo:
# composición intermedia (~525 MB) + copia del script de build
deck_work/
# plantilla de los organizadores (entrada, 106 MB)
Шаблон_Задача_3.pptx
# borrador anterior del deck
YOSOLO_stage1_pitch_ru.pptx
# volcado de inspección del deck
*.pptx.inspect.ndjson
# lock temporal de PowerPoint
~$*.pptx
# guion viejo; el vigente es docs/report/video_script.md (también ignorado)
SortMaster_pitch_script_ru.txt
# kit de grabación del video (material de trabajo, no entregable)
video_kit/
# notas internas de trabajo (prompts de agentes, planes de sesión) — no entregables
CROSS_BELT_REBUILD_PROMPT.md
GATED_ACTUATOR_TEST_PLAN.md
SUPER_REALISTIC_HACKATHON_PLAN.md
champion_level_improvement_agent.md
.cache/
